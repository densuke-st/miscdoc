name: format docs
on:
  push:
    branches:
      - main

jobs:

  build:
    runs-on: ubuntu-latest
    container:
      image: densuke/sphinx
      volumes:
        - docs:/work
    steps:
      - name: build
        run: |
          cd /work
          pwd
          ls -a
          make clean
          make html


